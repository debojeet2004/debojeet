import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['plus.unsplash.com', 'www.flaticon.com', 'images.unsplash.com'],
  },
  
};

export default nextConfig;
